// sample!!
const HOME = '/';
const JOIN = '/join';
const LOGIN = '/login';
const LOGOUT = '/logout';
const SEARCH = '/search';

const AUTH = '/auth';
const GET_TOKEN = '/getToken';

const routes = {
  home: HOME,
  join: JOIN,
  login: LOGIN,
  logout: LOGOUT,
  search: SEARCH,
  auth: AUTH,
  getToken: GET_TOKEN
};

export default routes;
